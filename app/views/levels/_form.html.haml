= form_with(model: level, class: 'modern-form') do |form|
  - if level.errors.any?
    .modern-alert.alert-danger{ role: 'alert' }
      %h2.mb-2= pluralize(level.errors.count, 'error') + ' prevented this level from being saved:'
      %ul.mb-0
        - level.errors.each do |error|
          %li= error.full_message

  .content-card
    .form-grid
      .form-group
        = form.label :name
        = form.text_field :name, class: 'form-control', placeholder: 'Internal level identifier'
      .form-group
        = form.label :title
        = form.text_field :title, class: 'form-control', placeholder: 'Visible level title'

  .form-actions
    = form.submit class: 'btn btn-primary'
