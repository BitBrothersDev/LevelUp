= render 'skills/add_learning_material', skill_level: skill_level, learning_material: material
.col-md-6
  .border.d-flex.align-items-center.p-3.my-2.text-white-50.bg-purple.rounded.shadow-sm.learn_item
    .lh-1
      %h1.h6.mb-0.text-white.lh-1= material.name.truncate(50)
      %small= material.material_type
    .ms-auto
      - if current_user.present?
        - checked = material.complete_material&.is_completed?
        = check_box_tag :checked, checked, checked = checked, {onclick: 'updateCompleteMaterial()', data: { material_id: material.id}}
      = link_to '-> Visit Link', material.link, class: 'btn btn-link text-white-50 fs-5'
      .btn.btn-link.text-white-50.fs-5{"data-bs-ta§rget" => "#modal-add-material-#{skill_level.id}-#{material.skill_level_item.id}-#{material.id}", "data-bs-toggle" => "modal", :type => "button"} Edit