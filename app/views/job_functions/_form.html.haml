= form_with(model: job_function, class: 'modern-form') do |form|
  - if job_function.errors.any?
    .modern-alert.alert-danger{ role: 'alert' }
      %h2.mb-2= pluralize(job_function.errors.count, 'error') + ' prevented this job function from being saved:'
      %ul.mb-0
        - job_function.errors.each do |error|
          %li= error.full_message

  .content-card
    .form-grid
      .form-group
        = form.label :title
        = form.text_field :title, class: 'form-control', placeholder: 'Enter a descriptive job function title'

  .form-actions
    = form.submit class: 'btn btn-primary'
